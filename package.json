{
  "name": "@mystikonetwork/node-lib",
  "version": "0.1.7",
  "description": "mystiko node lib",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mystikonetwork/mystiko-node.git"
  },
  "author": "eric",
  "license": "MIT",
  "workspaces": [
    "napi",
    "node"
  ],
  "scripts": {
    "artifacts": "yarn workspaces foreach -A run artifacts",
    "build": "yarn workspaces foreach -A run build",
    "prettier": "yarn workspaces foreach -A run prettier",
    "lint": "yarn workspaces foreach -A run lint",
    "test": "yarn workspaces foreach -A run test"
  },
  "packageManager": "yarn@4.0.2"
}
