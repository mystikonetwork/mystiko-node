{
  "name": "mystiko-node",
  "version": "0.2.0",
  "description": "mystiko node package",
  "private": true,
  "repository": "https://github.com/mystikonetwork/mystiko-node",
  "author": "eric",
  "license": "MIT",
  "workspaces": [
    "napi"
  ],
  "scripts": {
    "artifacts": "yarn workspaces foreach -A run artifacts",
    "build": "yarn workspaces foreach -A run build",
    "format": "yarn workspaces foreach -A run format",
    "lint": "yarn workspaces foreach -A run lint",
    "test": "yarn workspaces foreach -A run test"
  },
  "packageManager": "yarn@4.0.2"
}
